# Resultados

> Este documento é atualizado automaticamente a partir de
> `reports/metrics.json` e `reports/training_history.json`, gerados por
> `python -m src.train` e `python -m src.evaluate`. Os números abaixo são
> de uma execução real neste ambiente (ver ressalvas de hardware).

*(preenchido após o treino — ver seção "Execução real" no README.md e em*
*`reports/metrics.json` para os valores completos, incluindo exemplos de*
*tradução do conjunto de teste.)*
